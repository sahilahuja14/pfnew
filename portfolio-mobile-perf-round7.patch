diff --git a/client/src/App.css b/client/src/App.css
index 5a52cc5..3b43952 100644
--- a/client/src/App.css
+++ b/client/src/App.css
@@ -264,70 +264,30 @@ a {
   }
 }
 
-/* Base state: invisible until triggered */
-[data-card-reveal] {
-  /* 0.001, not 0: an element sitting at exactly opacity 0 gets its
-     backdrop-filter layer discarded by most browsers as a rendering
-     optimization, since fully-invisible content isn't worth compositing.
-     These cards can sit at opacity 0 for seconds while off-screen waiting
-     for their reveal, so when .card-in finally fires, the browser has to
-     build the blur layer from scratch in the same 0.55s window it's also
-     fading the text in — text paints instantly, blur visibly lags behind
-     while it "catches up". A hair above zero keeps the layer alive and
-     already-composited the whole time it's waiting, so the reveal is just
-     a cheap opacity/transform update on an already-warm layer instead of
-     a cold start. This is the same reason native UI blur (iOS's
-     UIVisualEffectView, for one) never shows this pop-in: the blur layer
-     is always live, only the content on top of it fades. */
-  opacity: 0.001;
-  transform: translateY(48px) scale(0.96);
-  /* will-change removed: this rule matches every project/skill/experience
-     card on the page, so it was telling the browser to keep a dedicated
-     GPU layer ready for ALL of them simultaneously, for the entire time
-     they sit off-screen waiting to be revealed — not just during their
-     one-time 0.55s reveal animation. Modern browsers already promote an
-     element to its own layer automatically once its animation starts,
-     so the hint here was pure overhead with no upside. */
-}
-
-[data-card-reveal].card-in {
-  animation: cardRevealIn 0.55s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
-}
-
-[data-card-reveal].card-out {
-  animation: cardRevealOut 0.4s cubic-bezier(0.55, 0, 1, 0.45) forwards;
-}
-
-/* Section pill headers — animate in from top */
-[data-pill-reveal] {
-  /* Same reasoning as [data-card-reveal] above — see that comment. */
-  opacity: 0.001;
-}
-
-[data-pill-reveal].pill-in {
-  animation: pillDrop 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
-}
-
-/* Skills tech tiles — staggered cascade */
-[data-tile-reveal] {
-  /* Same reasoning as [data-card-reveal] above — see that comment. */
-  opacity: 0.001;
-  transform: translateY(24px) scale(0.95);
-}
-
-[data-tile-reveal].tile-in {
-  animation: cascadeUp 0.45s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
-}
-
-/* Contact info / profile rows — slide in from left */
+/* Reveal-on-scroll removed for [data-card-reveal], [data-pill-reveal],
+   [data-tile-reveal], and [data-slide-left]: these attributes are still
+   present in the JSX (harmless — an IntersectionObserver still fires and
+   toggles a class), but nothing here reacts to that class anymore, so
+   cards/pills/tiles/rows just render in their normal final state
+   immediately. This was the recurring source of the backdrop-filter
+   pop-in: every version of the fix (warm-keeping opacity, fixing the
+   keyframes' own `from` values) still had SOME animation touching these
+   blurred elements, and each one turned out to have its own edge case.
+   Removing the animation entirely removes the whole category of bug. */
+[data-card-reveal],
+[data-pill-reveal],
+[data-tile-reveal],
 [data-slide-left] {
-  /* Same reasoning as [data-card-reveal] above — see that comment. */
-  opacity: 0.001;
-  transform: translateX(-24px);
+  opacity: 1;
+  transform: none;
 }
 
+[data-card-reveal].card-in,
+[data-card-reveal].card-out,
+[data-pill-reveal].pill-in,
+[data-tile-reveal].tile-in,
 [data-slide-left].slide-in {
-  animation: slideInLeft 0.45s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
+  animation: none;
 }
 
 /* ── Universal premium hover lift ── */
@@ -378,15 +338,12 @@ a {
 }
 
 .scroll-reveal {
-  /* Same reasoning as [data-card-reveal] further up this file, but this
-     one matters more: it's the outer wrapper for every section's entire
-     content (About, Experience, Skills, Projects, Contact), so every
-     blurred card nested inside inherits the "don't bother compositing"
-     treatment from the browser while this parent sits at opacity 0 —
-     regardless of what each individual card's own opacity says. */
-  opacity: 0.001;
-  transform: translateY(20px);
-  transition: opacity 1s ease-out, transform 1s ease-out;
+  /* Reveal-on-scroll removed entirely — content just renders in its
+     final state from the start. No opacity/transform transition means
+     nothing here ever triggers a backdrop-filter layer to be built or
+     rebuilt on scroll, which was the root of the recurring blur pop-in. */
+  opacity: 1;
+  transform: none;
 }
 
 .scroll-reveal.is-visible {
@@ -1094,18 +1051,11 @@ a {
   transition: border-color 400ms ease, transform 400ms ease;
   display: flex;
   flex-direction: column;
-  /* opacity 0.001, not 0 — see the [data-card-reveal] comment further up
-     this file. This class carries backdrop-filter directly, so it's one
-     of the more direct examples of the cold-start problem. */
-  opacity: 0.001;
-  /* will-change removed: it was set permanently here, which keeps the
-     browser holding a separate GPU-composited layer alive for every one
-     of these cards for the entire life of the page, not just the ~0.8s
-     they're actually animating in. will-change is a hint for an upcoming
-     change, not a general "make this fast" switch — leaving it on
-     indefinitely spends GPU memory/compositing budget for no benefit
-     once the reveal animation is done. The .slide-in-left/-right
-     animation classes below add it only while the animation runs. */
+  /* Reveal-on-scroll removed — see the block comment above
+     [data-card-reveal] earlier in this file for the full reasoning.
+     Renders visible immediately; .slide-in-right/-left below are now
+     no-ops. */
+  opacity: 1;
 }
 
 .about-floating-bento:hover {
@@ -1134,12 +1084,13 @@ a {
 }
 
 /* Animations for left/right sync */
-.slide-in-right {
-  animation: slideInRightAnim 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
-}
-
+.slide-in-right,
 .slide-in-left {
-  animation: slideInLeftAnim 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
+  /* Reveal-on-scroll removed — see [data-card-reveal] comment for the
+     full reasoning. These classes still get added by useElementReveal,
+     but no longer do anything, so the bentos never re-trigger a
+     backdrop-filter layer rebuild. */
+  animation: none;
 }
 
 @keyframes slideInRightAnim {
